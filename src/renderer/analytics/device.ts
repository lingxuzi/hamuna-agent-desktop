export {
  getAppVersion,
  getAppVersionSync,
  getDeviceId,
  getDeviceIdentity,
  getPlatform,
  preloadAppVersion,
  preloadDeviceId,
  preloadDeviceIdentity,
  preloadPlatform,
} from '@/identity/deviceIdentity';
